import reducer from './reducers';

import * as postOperations from './operatios';
import * as postTypes from './types';
import * as postSelectors from './selectors'

export {
    postOperations,
    postTypes,
    postSelectors
}

export default reducer;
