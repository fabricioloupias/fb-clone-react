import reducer from './reducers';

import * as stateOperations from './operations';
import * as stateTypes from './types';
import * as stateSelectors from './selectors'
import * as stateAction from './actions';

export {
    stateOperations,
    stateTypes,
    stateSelectors,
    stateAction
}

export default reducer;